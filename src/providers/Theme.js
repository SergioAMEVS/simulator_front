import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#018297'
    }
  },
  typography: {
    fontFamily: 'Lato, Arial, sans-serif'
  }
})
